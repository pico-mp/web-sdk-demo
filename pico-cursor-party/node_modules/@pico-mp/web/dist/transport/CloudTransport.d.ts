import type { ITransport } from './ITransport.js';
import type { CreateRoomOptions, RoomResult } from '../types.js';
/**
 * CloudTransport — connects to a real Game Server over HTTP + WebSocket.
 * Also used for LocalTransport (same protocol, different URL).
 *
 * Protocol reference: ADR 0002, Game_Server/src/http/routes.ts, ws/server.ts
 */
export declare class CloudTransport implements ITransport {
    private apiKey;
    private baseUrl;
    private wsBaseUrl;
    private playerId;
    private roomCode;
    private wsToken;
    private ws;
    private peers;
    private seq;
    private roomData;
    onPeerJoined: ((peerId: string) => void) | null;
    onPeerLeft: ((peerId: string) => void) | null;
    onPeerDisconnected: ((peerId: string) => void) | null;
    onPeerReconnected: ((peerId: string) => void) | null;
    onConnectionLost: (() => void) | null;
    onError: ((error: string, msg: string) => void) | null;
    onEnvelope: ((senderId: string, typeName: string, payload: unknown) => void) | null;
    onRoomDataChanged: ((key: string, value: unknown, setBy: string) => void) | null;
    constructor(apiKey: string, httpUrl: string);
    private post;
    createRoom(options?: CreateRoomOptions): Promise<RoomResult>;
    joinRoom(roomCode: string): Promise<void>;
    quickMatch(options?: import('../types.js').QuickMatchOptions): Promise<void>;
    leaveRoom(): Promise<void>;
    sendEnvelope(targetId: string | undefined, typeName: string, payload: unknown): void;
    setRoomData(key: string, value: unknown): void;
    getRoomData(key: string): unknown | undefined;
    getAllRoomData(): Record<string, unknown>;
    getPeers(): string[];
    getMyPlayerId(): string | null;
    private connectWs;
    private handleFrame;
    private closeWs;
    private reset;
}
//# sourceMappingURL=CloudTransport.d.ts.map